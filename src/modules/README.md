# Modules

NestJS modules that organize the application into cohesive functional features.

Contains domain modules (users, products, orders), feature modules (auth, notifications), and shared modules.